{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "npx prisma migrate resolve --rolled-back 20260223_phase1_core_enhancements || true && npx prisma migrate deploy && npx next start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
